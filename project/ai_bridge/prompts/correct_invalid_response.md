پاسخ قبلی شما برای یک وظیفه حسابداری با Schema سازگار نبود. آن را اصلاح کنید.

شناسه وظیفه: `{{task_id}}`
خطاهای اعتبارسنجی:
{{validation_errors}}

پاسخ قبلی:
{{previous_response}}

JSON Schema:
{{json_schema}}

فقط JSON اصلاح‌شده را برگردانید. هیچ توضیح، Markdown یا code fence ننویسید. شناسه وظیفه را تغییر ندهید.
